# heyFlorentin's Prettier Config

Prettier Docs: <https://prettier.io/>

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

Publish package:

```bash
bun publish
```

Using package:

```bash
bun add --dev @heyFlorentin/prettier-config
```
